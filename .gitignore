_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
.bundle/
vendor/
.impeccable/

# Written transiently by scripts/fetch-instagram.js, read once by the
# GitHub Actions workflow to push refreshed tokens to Secrets, then
# deleted — must never be committed even by accident.
scripts/refreshed-tokens.json
